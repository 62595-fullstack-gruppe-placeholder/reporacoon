services:
  web:
    build:
      context: ./reporacoonfrontend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://root:fisk@postgres/reporacoondb
    ports:
      - "3000:3000"
    depends_on:
      migrator:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    networks:
      - app-network


  scraper:
    build:
      context: ./scraper
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://root:fisk@postgres/reporacoondb
<<<<<<< HEAD
    ports:
      - "5001:5001"
=======
>>>>>>> main
    depends_on:
      migrator:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    networks:
      - app-network

  migrator:
    image: postgres:16-alpine
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGPASSWORD: fisk
      PGHOST: postgres
      PGUSER: root
      PGDATABASE: reporacoondb
    volumes:
      - ./migrations:/migrations:ro
    networks:
      - app-network
    command: >
      sh -c "
        echo 'Waiting for postgres...' &&
        until pg_isready -h $$PGHOST -p 5432 -U $$PGUSER; do 
          echo 'Postgres not ready, waiting...'; 
          sleep 2; 
        done &&
        echo 'Postgres ready! Running migrations...' &&
        files=$$(ls /migrations/*.sql 2>/dev/null || echo 'none') &&
        if [ '$$files' != 'none' ]; then
          for file in /migrations/*.sql; do
            echo \"Running $$file\" &&
            psql -h $$PGHOST -U $$PGUSER -d $$PGDATABASE -f $$file &&
            echo \"✓ Completed $$file\"
          done
        else
          echo 'No migration files found - skipping'
        fi &&
        echo 'All migrations completed successfully'
      "
    restart: "no"

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: reporacoondb
      POSTGRES_USER: root
      POSTGRES_PASSWORD: fisk
      POSTGRES_INITDB_ARGS: "--locale-provider=icu --icu-locale=root"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U root -d reporacoondb"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - app-network

volumes:
  postgres_data:

networks:
  app-network:
    driver: bridge
